---
import ToolLayout from '../../layouts/ToolLayout.astro';
import tools from '../../data/tools.json';
const tool = tools.find(t => t.slug === 'angular-acceleration-converter')!;
---

<ToolLayout tool={tool}>

  <div slot="tool">
    <h2 class="font-display seo-h2" style="margin-bottom:0.25rem;">Angular Acceleration Converter</h2>
    <p class="seo-p-sm" style="margin-bottom:1.25rem;">Enter an angular acceleration and select its unit — all others update instantly with visual gauges.</p>

    <!-- Input row -->
    <div style="display:grid;grid-template-columns:1fr auto;gap:0.75rem;align-items:end;margin-bottom:1.25rem;">
      <div>
        <label class="tool-label" for="angacc-input">Angular Acceleration</label>
        <input id="angacc-input" type="number" value="31.4159" step="any" placeholder="Enter value"
          class="input-themed"
          style="font-family:'Fira Code',monospace;font-weight:600;font-size:1.1rem;" />
      </div>
      <div>
        <label class="tool-label" for="angacc-unit">Unit</label>
        <select id="angacc-unit" class="select-themed" style="min-width:10rem;">
          <option value="rads2" selected>rad/s²</option>
          <option value="degs2">°/s²</option>
          <option value="rpms">RPM/s</option>
          <option value="revs2">rev/s²</option>
          <option value="hzs">Hz/s</option>
        </select>
      </div>
    </div>

    <!-- Visual gauges -->
    <div style="display:grid;grid-template-columns:auto 1fr;gap:1.5rem;align-items:center;margin-bottom:1.5rem;flex-wrap:wrap;">
      <!-- Tachometer gauge -->
      <svg viewBox="0 0 200 130" width="200" height="130" style="flex-shrink:0;">
        <defs>
          <linearGradient id="gaugeGrad" x1="0" y1="0" x2="1" y2="0">
            <stop offset="0%" stop-color="#22c55e" />
            <stop offset="60%" stop-color="#eab308" />
            <stop offset="100%" stop-color="#ef4444" />
          </linearGradient>
        </defs>
        <!-- Background arc -->
        <path d="M 20 115 A 80 80 0 0 1 180 115" fill="none" stroke="var(--border)" stroke-width="12" stroke-linecap="round" />
        <!-- Colored arc -->
        <path id="gauge-fill" d="M 20 115 A 80 80 0 0 1 180 115" fill="none" stroke="url(#gaugeGrad)" stroke-width="12" stroke-linecap="round" stroke-dasharray="251.2" stroke-dashoffset="251.2" />
        <!-- Needle -->
        <line id="gauge-needle" x1="100" y1="115" x2="100" y2="40" stroke="var(--text-primary)" stroke-width="2.5" stroke-linecap="round" />
        <circle cx="100" cy="115" r="5" fill="var(--text-primary)" />
        <!-- Value label -->
        <text id="gauge-val" x="100" y="108" text-anchor="middle" font-family="Fira Code, monospace" font-size="14" font-weight="700" fill="var(--text-primary)">0</text>
      </svg>
      <div style="min-width:0;">
        <p id="angacc-display" style="font-size:1.6rem;font-weight:700;font-family:'Fira Code',monospace;color:var(--text-primary);margin-bottom:0.25rem;">31.42 rad/s²</p>
        <p class="seo-p-sm" style="margin-bottom:0;">= <span id="angacc-rads2">31.4159</span> rad/s²</p>
        <p class="seo-p-sm">= <span id="angacc-degs">1800</span> °/s²</p>
      </div>
    </div>

    <!-- Bar chart -->
    <div style="margin-bottom:1.5rem;">
      <p class="text-secondary" style="font-size:0.78rem;font-weight:600;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:0.5rem;">Magnitude Comparison (rad/s²)</p>
      <svg id="angacc-chart" viewBox="0 0 600 250" width="100%" height="250" style="display:block;border-radius:0.5rem;background:var(--bg-surface);border:1.5px solid var(--border);"></svg>
    </div>

    <!-- Results -->
    <div id="results" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:0.5rem;"></div>

    <!-- Quick references -->
    <div style="margin-top:1.5rem;">
      <p class="text-secondary" style="font-size:0.78rem;font-weight:600;text-transform:uppercase;letter-spacing:0.05em;margin-bottom:0.5rem;">Quick References</p>
      <div id="refs" style="display:grid;grid-template-columns:repeat(auto-fill,minmax(220px,1fr));gap:0.375rem;"></div>
    </div>
  </div>

  <div slot="how-to-use">
    <h2 class="font-display seo-h2">How to Convert Between Rad/s², RPM/s, and Hz/s</h2>
    <p class="seo-p">
      A motor controller expects the ramp-up rate in rad/s², but your sensor outputs RPM/s. A conveyor motor programmed to reach 1,800 RPM in 3 seconds accelerates at 600 RPM/s. Type 600, select RPM/s, and see that it equals 62.83 rad/s², 10 Hz/s, and 22,619 deg/s². The tachometer gauge and bar chart show how this compares to real-world rotational accelerations.
    </p>
    <p class="seo-p">
      The converter handles 5 units: rad/s², deg/s², rev/s² (same as Hz/s), RPM/s, and Hz/s. A robotics engineer programming a pick-and-place arm at 500 rad/s² sees it equals 4,775 RPM/s and 79.58 Hz/s. A physics student measuring a disk accelerating from rest to 600 RPM in 2 seconds enters 300 RPM/s and sees 31.42 rad/s² for the torque calculation τ = Iα.
    </p>

    <h3 class="font-display seo-h3-sm">When the wrong unit breaks your control loop</h3>
    <div style="display:flex;flex-direction:column;gap:0.625rem;margin-bottom:0.75rem">
      {[
        ['VFD ramp rates use RPM/s but torque formulas need rad/s', 'A variable frequency drive is programmed with a ramp rate of 600 RPM/s. The control engineer needs rad/s² for the torque equation. 600 RPM/s = 62.83 rad/s². If the engineer enters 600 directly into a formula expecting rad/s², the calculated torque is 9.55x too large, and the motor oversizes by the same factor. The converter eliminates this error.'],
        ['Hard drive spin-up time requires precise Hz/s', 'A hard drive platter accelerates from 0 to 7,200 RPM in 0.5 seconds. That is 14,400 RPM/s = 240 Hz/s = 1,508 rad/s². The spin-up controller uses Hz/s to time the read/write head activation. If the head activates before the platter reaches speed, data corruption occurs. The converter helps verify the exact spin-up rate.'],
        ['Robot joint limits are specified in rad/s²', 'A collaborative robot arm has a maximum joint acceleration of 50 rad/s² for safety. The motor datasheet specifies torque in N·m and expects α in rad/s². If you accidentally use deg/s² (50 deg/s² = 0.873 rad/s²), the robot moves 57x slower than intended. If you use RPM/s (50 RPM/s = 5.24 rad/s²), the robot exceeds safety limits by 10x.']
      ].map(([title, desc]) => (
        <div class="border-theme surface-2 seo-card-sm">
          <strong style="color:var(--text-primary);font-size:0.875rem">{title}</strong>
          <p style="font-size:0.85rem;line-height:1.65;margin:0.35rem 0 0">{desc}</p>
        </div>
      ))}
    </div>
  </div>

  <div slot="how-it-works">
    <h2 class="font-display seo-h2">How the Angular Acceleration Converter Handles Rad/s² and RPM/s</h2>
    <p class="seo-p">
      All conversions pass through radians per second squared (rad/s²). When you enter 600 RPM/s, the tool multiplies by (2π)/60 = π/30 = 0.10472 to get 62.83 rad/s². Then it divides by each target unit's factor: by π/180 for deg/s², by 2π for Hz/s, by 1 for rad/s². The result: 600 RPM/s = 62.83 rad/s² = 3,600 deg/s² = 10 Hz/s.
    </p>

    <h3 class="font-display seo-h3-sm">Why RPM/s and rad/s² differ by a factor of 9.55</h3>
    <p class="seo-p-sm">
      One revolution equals 2π radians (6.28319). RPM/s counts full revolutions per second per second. rad/s² counts radians per second per second. The conversion factor is 2π/60 = 0.10472 for RPM/s to rad/s², or equivalently 60/(2π) = 9.5493 for rad/s² to RPM/s. This factor is irrational (involving π), so it is stored as an IEEE 754 double-precision approximation with 15-17 significant digits.
    </p>

    <h3 class="font-display seo-h3-sm">The tachometer gauge and how it scales</h3>
    <p class="seo-p-sm">
      The SVG tachometer shows your value on a semicircular arc. The needle position is proportional to your value relative to the maximum reference (jet turbine at ~5,000 rad/s²). The arc uses stroke-dasharray to show filled and unfilled portions. If your value exceeds the maximum reference, the needle pins to the right. If your value is very small, the needle stays near the left.
    </p>
    <p class="seo-p-none">
      The bar chart below the gauge compares your value to 6 reference accelerations: turntable start-up (2 rad/s²), blender (50 rad/s²), car engine (209 rad/s²), hard drive (1,508 rad/s²), centrifuge (5,236 rad/s²), and jet turbine (20,944 rad/s²). Each bar scales linearly. Your bar is highlighted in blue while references are grey. This gives immediate visual context for whether your angular acceleration is modest or extreme.
    </p>
  </div>

  <div slot="faq">
    <h2 class="font-display seo-h2-faq">Frequently Asked Questions</h2>
    <div style="display:flex;flex-direction:column;gap:0.75rem" itemscope itemtype="https://schema.org/FAQPage">
      {[
        {
          q: "How do I convert 2000 RPM/s to rad/s²?",
          a: "Multiply by π/30 (approximately 0.10472): 2000 x 0.10472 = 209.44 rad/s². This is the rate at which a car engine accelerates from idle to redline in about 3 seconds. For Hz/s, divide by 60: 2000 / 60 = 33.33 Hz/s. The converter shows all units simultaneously so you can verify the relationships."
        },
        {
          q: "What is the angular acceleration of a hard drive spin-up?",
          a: "A typical 7,200 RPM hard drive reaches full speed in about 0.5 seconds. That is 14,400 RPM/s = 240 Hz/s = 1,508 rad/s². A 10,000 RPM drive spinning up in 0.8 seconds achieves 12,500 RPM/s = 208.3 Hz/s = 1,309 rad/s². The spin-up controller must time the read/write head activation precisely to avoid data corruption."
        },
        {
          q: "Why does the formula τ = Iα require rad/s²?",
          a: "Newton's second law for rotation (τ = Iα) only works when α is in rad/s² because the radian is a dimensionless ratio. If you use RPM/s or deg/s², you get wrong torque values. For example: I = 2 kg·m², α = 600 RPM/s. The correct torque is τ = 2 x 62.83 = 125.66 N·m. Using 600 directly gives τ = 1200 N·m, which is 9.55x too large."
        },
        {
          q: "How is angular acceleration different from angular velocity?",
          a: "Angular velocity (ω) measures how fast something rotates (rad/s). Angular acceleration (α) measures how fast the rotation speed changes (rad/s²). A motor running at constant 3000 RPM has ω = 314.16 rad/s but α = 0. If it speeds up from 0 to 3000 RPM in 2 seconds, α = 157.08 rad/s². Acceleration is the derivative of velocity."
        },
        {
          q: "What is a realistic angular acceleration for a car engine?",
          a: "A typical car engine accelerates from idle (1,000 RPM) to redline (7,000 RPM) in about 3 seconds. That is 2,000 RPM/s = 209.44 rad/s² = 33.33 Hz/s. A high-performance engine might do this in 1.5 seconds: 4,000 RPM/s = 418.88 rad/s². Formula 1 engines reach 18,000 RPM from idle in under 0.5 seconds: over 30,000 RPM/s."
        },
        {
          q: "How do I measure angular acceleration experimentally?",
          a: "Use a rotary motion sensor connected to a data logger. Record angular position or velocity over time. Compute α = Δω / Δt from the slope of the velocity-time graph. If a disk goes from 0 to 600 RPM in 2 seconds, the average α is 300 RPM/s = 31.42 rad/s². For instantaneous α, take the derivative of the velocity curve at each point."
        }
      ].map(item => (
        <div itemscope itemprop="mainEntity" itemtype="https://schema.org/Question"
          class="surface-2 border-theme seo-faq-card">
          <h3 itemprop="name" class="seo-faq-q">{item.q}</h3>
          <div itemscope itemprop="acceptedAnswer" itemtype="https://schema.org/Answer">
            <p itemprop="text" class="seo-faq-a">{item.a}</p>
          </div>
        </div>
      ))}
    </div>
  </div>

</ToolLayout>

<script>
(function () {
  // ── Conversion factors to rad/s² ────────────────────────────────
  const TO_RADS2 = {
    rads2: 1,
    degs2: Math.PI / 180,
    rpms:  (2 * Math.PI) / 60,
    revs2: 2 * Math.PI,
    hzs:   2 * Math.PI,
  };

  const UNITS = [
    { id: 'rads2', label: 'rad/s²',  name: 'Radian per Second²' },
    { id: 'degs2', label: '°/s²',    name: 'Degree per Second²' },
    { id: 'rpms',  label: 'RPM/s',   name: 'RPM per Second' },
    { id: 'revs2', label: 'rev/s²',  name: 'Revolution per Second²' },
    { id: 'hzs',   label: 'Hz/s',    name: 'Hertz per Second' },
  ];

  const REFS = [
    { name: 'Earth Rotation Speedup',  rads2: 0.000073 },
    { name: 'Turntable (start)',        rads2: 0.5 },
    { name: 'Blender (start)',          rads2: 150 },
    { name: 'Car Engine (idle→redline)',rads2: 209 },
    { name: 'Centrifuge (spin-up)',     rads2: 175 },
    { name: 'Jet Turbine (start)',      rads2: 2500 },
    { name: 'Drill (start)',            rads2: 15000 },
    { name: 'Hard Drive (start)',       rads2: 45000 },
  ];

  const BAR_DATA = [
    { name: 'Turntable',      rads2: 0.5 },
    { name: 'Blender',        rads2: 150 },
    { name: 'Car Engine',     rads2: 209 },
    { name: 'Centrifuge',     rads2: 175 },
    { name: 'Jet Turbine',    rads2: 2500 },
    { name: 'Hard Drive',     rads2: 45000 },
  ];

  const input    = document.getElementById('angacc-input') as HTMLInputElement;
  const select   = document.getElementById('angacc-unit') as HTMLSelectElement;
  const grid     = document.getElementById('results')!;
  const refsDiv  = document.getElementById('refs')!;
  const chart    = document.getElementById('angacc-chart')!;
  const gaugeFill = document.getElementById('gauge-fill')!;
  const gaugeNeedle = document.getElementById('gauge-needle')!;
  const gaugeVal = document.getElementById('gauge-val')!;
  const angaccDisp = document.getElementById('angacc-display')!;
  const angaccRads = document.getElementById('angacc-rads2')!;
  const angaccDegs = document.getElementById('angacc-degs')!;

  const ARC_TOTAL = 251.2; // approximate arc length of semicircle r=80

  function fmt(value: number): string {
    if (Number.isNaN(value)) return '—';
    if (value === 0) return '0';
    const abs = Math.abs(value);
    if (abs >= 1e12) return value.toLocaleString('en-US', { maximumFractionDigits: 2 });
    if (abs >= 1e6)  return value.toLocaleString('en-US', { maximumFractionDigits: 2 });
    if (abs >= 1000) return value.toLocaleString('en-US', { maximumFractionDigits: 4 });
    if (abs >= 1)    return value.toLocaleString('en-US', { maximumFractionDigits: 6 });
    if (abs >= 0.001) return value.toLocaleString('en-US', { maximumFractionDigits: 8 });
    return value.toExponential(4);
  }

  function drawGauge(rads2: number) {
    const maxRef = Math.max(...REFS.map(r => r.rads2), rads2) * 1.2;
    const fraction = Math.min(rads2 / maxRef, 1);
    const offset = ARC_TOTAL * (1 - fraction);
    gaugeFill.setAttribute('stroke-dashoffset', String(offset));

    // Needle rotation: -90 (left) to +90 (right)
    const angle = -90 + fraction * 180;
    const rad = angle * Math.PI / 180;
    const nx = 100 + 70 * Math.sin(rad);
    const ny = 115 - 70 * Math.cos(rad);
    gaugeNeedle.setAttribute('x2', String(nx));
    gaugeNeedle.setAttribute('y2', String(ny));

    gaugeVal.textContent = fmt(rads2);
  }

  function drawChart(userRads2: number) {
    chart.innerHTML = '';
    const pad = { top: 10, bottom: 10, left: 130, right: 60 };
    const svgW = 600;
    const svgH = 250;
    const w = svgW - pad.left - pad.right;
    const rows = BAR_DATA.length + 1;
    const rowH = (svgH - pad.top - pad.bottom) / rows;
    const barH = rowH * 0.55;

    const maxVal = Math.max(userRads2, ...BAR_DATA.map(b => b.rads2)) * 1.2;

    function yPos(i: number) {
      return pad.top + i * rowH + (rowH - barH) / 2;
    }

    // User bar
    const uW = Math.max((userRads2 / maxVal) * w, 2);
    const rect = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
    rect.setAttribute('x', String(pad.left));
    rect.setAttribute('y', String(yPos(0)));
    rect.setAttribute('width', String(uW));
    rect.setAttribute('height', String(barH));
    rect.setAttribute('rx', '3');
    rect.setAttribute('fill', 'var(--color-brand-blue)');
    chart.appendChild(rect);

    const label = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    label.setAttribute('x', String(pad.left - 8));
    label.setAttribute('y', String(yPos(0) + barH / 2 + 4));
    label.setAttribute('text-anchor', 'end');
    label.setAttribute('fill', 'var(--color-brand-blue)');
    label.setAttribute('font-size', '11');
    label.setAttribute('font-weight', '600');
    label.setAttribute('font-family', 'Sora, sans-serif');
    label.textContent = 'Your Value';
    chart.appendChild(label);

    const valText = document.createElementNS('http://www.w3.org/2000/svg', 'text');
    valText.setAttribute('x', String(pad.left + uW + 6));
    valText.setAttribute('y', String(yPos(0) + barH / 2 + 4));
    valText.setAttribute('fill', 'var(--text-secondary)');
    valText.setAttribute('font-size', '10');
    valText.setAttribute('font-family', 'Fira Code, monospace');
    valText.textContent = fmt(userRads2) + ' rad/s²';
    chart.appendChild(valText);

    // Reference bars
    BAR_DATA.forEach((b, i) => {
      const idx = i + 1;
      const bw = Math.max((b.rads2 / maxVal) * w, 2);

      const r = document.createElementNS('http://www.w3.org/2000/svg', 'rect');
      r.setAttribute('x', String(pad.left));
      r.setAttribute('y', String(yPos(idx)));
      r.setAttribute('width', String(bw));
      r.setAttribute('height', String(barH));
      r.setAttribute('rx', '3');
      r.setAttribute('fill', 'var(--border)');
      chart.appendChild(r);

      const t = document.createElementNS('http://www.w3.org/2000/svg', 'text');
      t.setAttribute('x', String(pad.left - 8));
      t.setAttribute('y', String(yPos(idx) + barH / 2 + 4));
      t.setAttribute('text-anchor', 'end');
      t.setAttribute('fill', 'var(--text-secondary)');
      t.setAttribute('font-size', '10');
      t.setAttribute('font-family', 'Sora, sans-serif');
      t.textContent = b.name;
      chart.appendChild(t);

      const v = document.createElementNS('http://www.w3.org/2000/svg', 'text');
      v.setAttribute('x', String(pad.left + bw + 6));
      v.setAttribute('y', String(yPos(idx) + barH / 2 + 4));
      v.setAttribute('fill', 'var(--text-muted)');
      v.setAttribute('font-size', '9');
      v.setAttribute('font-family', 'Fira Code, monospace');
      v.textContent = fmt(b.rads2) + ' rad/s²';
      chart.appendChild(v);
    });
  }

  function update() {
    const val      = parseFloat(input.value);
    const fromUnit = select.value;
    const rads2    = val * TO_RADS2[fromUnit];

    const unitLabel = UNITS.find(u => u.id === fromUnit)!.label;
    angaccDisp.textContent = `${fmt(val)} ${unitLabel}`;
    angaccRads.textContent = fmt(rads2);
    angaccDegs.textContent = fmt(rads2 * 180 / Math.PI);

    drawGauge(rads2);
    drawChart(rads2);

    grid.innerHTML = '';

    UNITS.forEach(u => {
      if (u.id === fromUnit) return;
      const converted = rads2 / TO_RADS2[u.id];
      const display = fmt(converted);
      const row = document.createElement('div');
      row.className = 'info-row';
      row.innerHTML = `
        <span class="info-label">${u.label}</span>
        <div style="display:flex;align-items:center;gap:0.5rem;flex:1;justify-content:flex-end;min-width:0;">
          <span class="info-value" style="word-break:break-all;" data-val="${display}">${display}</span>
          <button class="btn-secondary copy-btn" type="button" style="font-size:0.7rem;padding:0.2rem 0.55rem;flex-shrink:0;">Copy</button>
        </div>
      `;
      row.querySelector<HTMLButtonElement>('.copy-btn')!.addEventListener('click', () => {
        const btn = row.querySelector<HTMLButtonElement>('.copy-btn')!;
        navigator.clipboard.writeText(display).then(() => {
          btn.textContent = '✓';
          btn.classList.add('btn-success');
          setTimeout(() => { btn.textContent = 'Copy'; btn.classList.remove('btn-success'); }, 2000);
        });
      });
      grid.appendChild(row);
    });

    // Quick references
    refsDiv.innerHTML = '';
    REFS.forEach(r => {
      const value = r.rads2 / TO_RADS2[fromUnit];
      const display = fmt(value);
      const row = document.createElement('div');
      row.className = 'info-row';
      row.style.padding = '0.5rem 0.75rem';
      row.innerHTML = `
        <span class="info-label" style="min-width:auto;">${r.name}</span>
        <span class="info-value" style="font-size:0.82rem;">${display} ${unitLabel}</span>
      `;
      refsDiv.appendChild(row);
    });
  }

  input.addEventListener('input', update);
  select.addEventListener('change', update);
  update();
})();
</script>
